# TODO

- [x] Update Vite proxy target so host-based `npm run dev` can reach backend
- [ ] Ensure proxy works for `/api/*` calls (signup/login)
- [ ] Run quick sanity check: start client dev server and retry signup

